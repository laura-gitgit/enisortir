{% extends 'base.html.twig' %}

{% block body %}

    <div>
    <h2>{{ profil.pseudo }}</h2>
    </div>

    <img>
        <img src="{{ asset('images/avatar/'~profil.image) }}" alt="" height="150" width="150">
        <p>Prénom : {{ profil.prenom }}</p>
        <p>Nom : {{ profil.nom }}</p>
        <p>Téléphone : {{ profil.telephone }}</p>
        <p>Email : {{ profil.email }}</p>
        <p>Ville de rattachement : {{ profil.site.nom }}</p>
    </div>

    <div><a href="{{ path('_sorties') }}"><button>Retour</button></a> </div>


{% endblock %}

{% block title %}

{% endblock %}

