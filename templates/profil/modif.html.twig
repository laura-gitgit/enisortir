{% extends 'base.html.twig' %}

{% block body %}

    <h2>Mon profil</h2>
    <div>

        <img src="{{ asset('images/avatar/'~profil.image) }}" alt="" height="150" width="150">

        {{ form_start(profilForm) }}
        {{ form_row(profilForm.pseudo) }}
        {{ form_row(profilForm.prenom) }}
        {{ form_row(profilForm.nom) }}
        {{ form_row(profilForm.telephone) }}
        {{ form_row(profilForm.email) }}
        {{ form_row(profilForm.password) }}
        {{ form_row(profilForm.imageFile) }}
        {{ form_row(profilForm.site) }}
        <button>Enregistrer</button>
        {{ form_end(profilForm) }}
        <a href="{{ path('_sorties') }}"><button>Annuler</button></a>
    </div>

{% endblock %}

{% block title %}

{% endblock %}

